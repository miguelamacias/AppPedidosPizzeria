<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.macisdev.apppedidospizzeria">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/pizza_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pizza_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MakeOrderActivity"
            android:label="@string/make_order_activity"/>
        <activity android:name=".CustomizePizzaActivity" />
        <activity
            android:name=".OrderSummaryActivity"
            android:label="@string/show_order_summary"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PizzaDetailsActivity"
            android:parentActivityName=".PizzasActivity"
            android:label="@string/pizza_details_activity"/>
        <activity
            android:name=".PizzasActivity"
            android:label="@string/pizzas_menu"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>